# makefile for dvips

text.dvi: text.tex
	latex text.tex

text.ps: text.dvi
	dvips -Ppdf -G0 text.dvi
	# the -G0 fixes the ligature problem.

text.pdf: text.ps
	ps2pdf text.ps

PDF: text.pdf
	xpdf text.pdf
